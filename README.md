# Habit Tracker Assessment

A lightweight, single-page habit tracker built with Vanilla HTML, CSS, and JavaScript.

## How to run locally
There are no dependencies, build steps, or frameworks required. 
Simply double-click the `index.html` file to open it in any modern web browser, or use the VS Code "Live Server" extension.